<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🍳 Département Culinaire</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #fff3e0;
            font-family: 'Poppins', sans-serif;
            color: #333;
        }

        .container {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            text-align: center;
        }

        h1 {
            font-size: 2.5rem;
            font-weight: 700;
            color: #e65100;
            margin-bottom: 30px;
        }

        .cookie-jar {
            width: 200px;
            height: 200px;
            object-fit: cover;
            margin: 20px auto;
            border-radius: 50%;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }

        .btn-warning {
            background-color: #ffb300;
            color: white;
            padding: 12px 25px;
            font-size: 1.2rem;
            font-weight: bold;
            border-radius: 8px;
            transition: background-color 0.3s ease;
        }

        .btn-warning:hover {
            background-color: #f57c00;
        }

        .alert-success {
            margin-top: 20px;
            background-color: #388e3c;
            color: white;
            font-weight: bold;
            border-radius: 8px;
            padding: 10px;
        }

        .btn-custom {
            background-color: #ff6f00;
            color: white;
            padding: 12px 20px;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: bold;
            text-transform: uppercase;
            margin-top: 20px;
            transition: background-color 0.3s ease;
        }

        .btn-custom:hover {
            background-color: #e65100;
        }

        .error-message {
            background: #ffcccc; /* Rouge clair pour signaler une erreur */
            color: #d32f2f; /* Rouge foncé pour le texte */
            padding: 15px;
            border: 1px solid #d32f2f;
            border-radius: 8px;
            margin-top: 15px;
            font-size: 1rem;
            font-weight: bold;
            box-shadow: 0px 4px 8px rgba(211, 47, 47, 0.2);
            animation: fadeIn 0.5s ease-in-out;
        }

        .flag-message {
            background: #ccffcc; 
            color: #2e7d32;
            padding: 15px;
            border: 1px solid #2e7d32;
            border-radius: 8px;
            margin-top: 15px;
            font-size: 1rem;
            font-weight: bold;
            box-shadow: 0px 4px 8px rgba(46, 125, 50, 0.2);
            animation: fadeIn 0.5s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .btn-back {
            position: absolute;
            top: 20px;
            left: 20px;
            background-color: #0277bd;
            color: white;
            padding: 10px 20px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: bold;
            transition: background-color 0.3s ease;
        }

        .btn-back:hover {
            background-color: #01579b;
        }

    </style>
</head>
<body>
    <a href="/index.html" class="btn-back">Retour à l'accueil</a>

    <div class="container">
        <h1>🍪 Préparation de cookies</h1>
        <img src="https://images.unsplash.com/photo-1558961363-fa8fdf82db35?ixlib=rb-1.2.1&auto=format&fit=crop&w=400&q=80" class="cookie-jar rounded-circle">
        
        <button onclick="startCooking()" class="btn btn-warning btn-lg mt-3">
            Commencer la préparation
        </button>

        <% if (cookies.preparation && cookies.cuisson) { %>
            <div class="flag-message">Part 4: 4nD_C00k13_Ch3f}</div>
        <% } else if (cookies.preparation) { %>
            <div class="error-message">Votre cookie manque de cuisson</div>
        <% } %>

        <script>
            function startCooking() {
                fetch('/set-cookies')
                    .then(() => location.reload());
            }
        </script>
    </div>
</body>
</html>
