<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🕵️ Département Renseignement</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: #2d2d2d;
            color: white;
            font-family: 'Poppins', sans-serif;
        }

        .container {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            text-align: center;
        }

        h1 {
            font-size: 2.5rem;
            font-weight: 700;
            color: #ff1744;
            margin-bottom: 30px;
        }

        .classified {
            border: 3px solid red;
            padding: 20px;
            background-color: rgba(255, 0, 0, 0.1);
            border-radius: 10px;
            font-size: 1.2rem;
            font-weight: bold;
        }

        .btn-custom {
            background-color: #ff1744;
            color: white;
            padding: 12px 20px;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: bold;
            text-transform: uppercase;
            margin-top: 20px;
            transition: background-color 0.3s ease;
        }

        .btn-custom:hover {
            background-color: #d50000;
        }

    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-danger">⚠ DOCUMENT CLASSÉ CONFIDENTIEL ⚠</h1>
        <% if (is007) { %>
            <div class="classified">Part 3: Us3r_4g3nt_007_</div>
        <% } else { %>
            <p class="lead">L'agent "007" est requis pour accéder à ce document</p>
        <% } %>
        <a href="/index.html" class="btn btn-custom">Retour à l'accueil</a>
    </div>
</body>
</html>
